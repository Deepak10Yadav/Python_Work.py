from typing import List,Tuple,Dict,Union  #----->>>> Main Function of  type function

number: List[int] = [1,2,3,4,5]
number1: Dict[str,int] = {"vipin":2}
scores: tuple[str,int] = ("Deepak",2)
indentifier: Union[str,int] = "ID536"


n : int = 3
name: str = "Vipin"


def sum (a:int,b:int) -> int:
    return a+b

# by using type function it becomes easy for other coder to unerstand the datatype of codes. 
